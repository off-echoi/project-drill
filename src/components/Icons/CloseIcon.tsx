import { RiCloseLine } from 'react-icons/ri'
import { IconBaseProps } from 'react-icons'

export const CloseIcon = ({ size = '30', color = '#555' }: IconBaseProps) => {
  return <RiCloseLine size={size} color={color} />
}
