import { RiAddFill } from 'react-icons/ri'
import { IconBaseProps } from 'react-icons'

export const AddIcon = ({ size = '30', color = '#555' }: IconBaseProps) => {
  return <RiAddFill size={size} color={color} />
}
