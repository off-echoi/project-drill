import { RiMenuFill } from 'react-icons/ri'
import { IconBaseProps } from 'react-icons'

export const MenuIcon = ({ size = '30', color = '#555' }: IconBaseProps) => {
  return <RiMenuFill size={size} color={color} />
}
