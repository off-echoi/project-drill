import { RiSearchLine } from 'react-icons/ri'
import { IconBaseProps } from 'react-icons'

export const SearchIcon = ({ size = '30', color = '#555' }: IconBaseProps) => {
  return <RiSearchLine size={size} color={color} />
}
