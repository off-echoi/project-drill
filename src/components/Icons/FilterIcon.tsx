import { RiFilter2Line } from 'react-icons/ri'
import { IconBaseProps } from 'react-icons'

export const FilterIcon = ({ size = '30', color = '#555' }: IconBaseProps) => {
  return <RiFilter2Line size={size} color={color} />
}
