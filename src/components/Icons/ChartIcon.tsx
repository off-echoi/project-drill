import { RiLineChartLine } from 'react-icons/ri'
import { IconBaseProps } from 'react-icons'

export const ChartIcon = ({ size = '30', color = '#555' }: IconBaseProps) => {
  return <RiLineChartLine size={size} color={color} />
}
