import { RiDeleteBinLine } from 'react-icons/ri'
import { IconBaseProps } from 'react-icons'

export const DeleteIcon = ({ size = '30', color = '#555' }: IconBaseProps) => {
  return <RiDeleteBinLine size={size} color={color} />
}
