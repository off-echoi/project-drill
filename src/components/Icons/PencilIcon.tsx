import { RiPencilLine } from 'react-icons/ri'
import { IconBaseProps } from 'react-icons'

export const PencilIcon = ({ size = '30', color = '#555' }: IconBaseProps) => {
  return <RiPencilLine size={size} color={color} />
}
