import { RiUpload2Line } from 'react-icons/ri'
import { IconBaseProps } from 'react-icons'

export const UploadIcon = ({ size = '30', color = '#555' }: IconBaseProps) => {
  return <RiUpload2Line size={size} color={color} />
}
