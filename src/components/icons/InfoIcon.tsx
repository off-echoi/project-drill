import { RiInformationLine } from 'react-icons/ri'
import { IconBaseProps } from 'react-icons'

export const InfoIcon = ({ size = '30', color = '#555' }: IconBaseProps) => {
  return <RiInformationLine size={size} color={color} />
}
