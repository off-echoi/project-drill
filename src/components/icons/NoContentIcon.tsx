import { RiMenuSearchLine } from 'react-icons/ri';
import { IconBaseProps } from 'react-icons';

export const NoContentIcon = ({ size = '30', color = '#555' }: IconBaseProps) => {
  return <RiMenuSearchLine size={size} color={color} />;
};
