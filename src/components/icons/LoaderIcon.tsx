import { RiLoader4Line } from 'react-icons/ri'
import { IconBaseProps } from 'react-icons'

export const LoaderIcon = ({ size = '30', color = '#555' }: IconBaseProps) => {
  return <RiLoader4Line size={size} color={color} />
}
